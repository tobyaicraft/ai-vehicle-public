# TC275 Lite Kit - ADC 테스트 구현

**날짜**: 2026-03-25
**보드**: AURIX TC275 Lite Kit V1.1
**목표**: 온보드 포텐시오미터(AN0)를 이용한 VADC ADC 테스트

---

## 개요

TC275 Lite Kit 보드에 장착된 10k 포텐시오미터(AN0)의 아날로그 값을 VADC로 읽어,
ADC 값에 따라 LED1(P00.5)의 점멸 속도를 제어하는 테스트 프로그램.

- **ADC 모드**: Background Scan (연속 변환)
- **해상도**: 12-bit (0 ~ 4095)
- **읽기 주기**: 10ms
- **LED 제어 주기**: 100ms

---

## 추가/수정 파일

| 파일 | 상태 | 설명 |
|------|------|------|
| `Drv_Adc.h` | 신규 | ADC 드라이버 헤더 |
| `Drv_Adc.c` | 신규 | VADC Group0 Background Scan 초기화 + AN0 결과 읽기 |
| `Cpu0_Main.c` | 수정 | `DrvAdc_Init()` 호출 추가 |
| `App_Scheduler.c` | 수정 | 10ms 태스크에서 ADC 읽기, 100ms 태스크에서 LED 점멸 속도 제어 |

---

## 소스 코드

### Drv_Adc.h

```c
#ifndef DRV_ADC_H
#define DRV_ADC_H

#include "Ifx_Types.h"

void DrvAdc_Init(void);
uint16 DrvAdc_GetResult_AN0(void);

#endif /* DRV_ADC_H */
```

### Drv_Adc.c

```c
#include "Drv_Adc.h"
#include "Vadc/Adc/IfxVadc_Adc.h"

static IfxVadc_Adc vadc;
static IfxVadc_Adc_Group adcGroup0;
static IfxVadc_Adc_Channel adcChannel_AN0;

void DrvAdc_Init(void)
{
    /* Module configuration */
    IfxVadc_Adc_Config adcConfig;
    IfxVadc_Adc_initModuleConfig(&adcConfig, &MODULE_VADC);
    IfxVadc_Adc_initModule(&vadc, &adcConfig);

    /* Group 0 configuration - Background Scan mode */
    IfxVadc_Adc_GroupConfig groupConfig;
    IfxVadc_Adc_initGroupConfig(&groupConfig, &vadc);

    groupConfig.groupId = IfxVadc_GroupId_0;
    groupConfig.master  = IfxVadc_GroupId_0;

    groupConfig.arbiter.requestSlotBackgroundScanEnabled = TRUE;
    groupConfig.backgroundScanRequest.autoBackgroundScanEnabled = TRUE;
    groupConfig.backgroundScanRequest.triggerConfig.gatingMode = IfxVadc_GatingMode_always;

    IfxVadc_Adc_initGroup(&adcGroup0, &groupConfig);

    /* Channel 0 (AN0) - potentiometer on TC275 Lite Kit */
    IfxVadc_Adc_ChannelConfig chConfig;
    IfxVadc_Adc_initChannelConfig(&chConfig, &adcGroup0);

    chConfig.channelId       = IfxVadc_ChannelId_0;
    chConfig.resultRegister  = IfxVadc_ChannelResult_0;
    chConfig.backgroundChannel = TRUE;

    IfxVadc_Adc_initChannel(&adcChannel_AN0, &chConfig);

    /* Add AN0 to background scan */
    unsigned channels = (1 << IfxVadc_ChannelId_0);
    IfxVadc_Adc_setBackgroundScan(&vadc, &adcGroup0, channels, channels);

    /* Start continuous background scan */
    IfxVadc_Adc_startBackgroundScan(&vadc);
}

uint16 DrvAdc_GetResult_AN0(void)
{
    Ifx_VADC_RES result;
    result = IfxVadc_Adc_getResult(&adcChannel_AN0);

    if (result.B.VF)
    {
        return (uint16)result.B.RESULT;
    }

    return 0;
}
```

### Cpu0_Main.c (수정 부분)

```c
#include "Drv_Int.h"
#include "Drv_Dio.h"
#include "Drv_Stm.h"
#include "Drv_Adc.h"        // 추가
#include "App_Scheduler.h"

void core0_main(void)
{
    DrvInt_Init();
    DrvDio_Init();
    DrvAdc_Init();           // 추가
    DrvSTM_Init();
    Scheduler_init();
    IfxCpu_enableInterrupts();

    while(1)
    {
        Scheduler_run();
    }
}
```

### App_Scheduler.c (수정 부분)

```c
#include "Drv_Adc.h"         // 추가

static volatile uint16 adcValue_AN0;  // 추가

static void Task_10ms(void)
{
    adcValue_AN0 = DrvAdc_GetResult_AN0();  // 10ms마다 ADC 읽기
}

static void Task_100ms(void)
{
    static uint8 ledCounter = 0;

    /* ADC 12-bit (0~4095) -> LED toggle period: 1~20 (x100ms)
     * Pot left (0):    period = 1  -> fast blink (100ms)
     * Pot right (4095): period = 20 -> slow blink (2s)
     */
    uint8 period = (uint8)(adcValue_AN0 / 215) + 1;  /* 4095/19 ≈ 215 */
    if (period > 20)
    {
        period = 20;
    }

    ledCounter++;

    if (ledCounter >= period)
    {
        ledCounter = 0;
        IfxPort_togglePin(&MODULE_P00, 5);
    }
}
```

---

## 동작 설명

```
포텐시오미터(AN0)
    │
    ▼
VADC Group0, Ch0 (Background Scan, 연속 변환)
    │
    ▼
Task_10ms() ── adcValue_AN0 업데이트 (12-bit: 0~4095)
    │
    ▼
Task_100ms() ── ADC 값에 따라 LED 점멸 주기 결정
    │
    ├─ ADC = 0    (왼쪽) → period = 1  → 100ms 간격 점멸 (빠름)
    ├─ ADC = 2048 (중앙) → period = 10 → 1초 간격 점멸
    └─ ADC = 4095 (오른쪽) → period = 20 → 2초 간격 점멸
```

---

## 하드웨어 연결

TC275 Lite Kit 보드에 이미 연결되어 있음 (별도 배선 불필요):

| 기능 | 핀 | 비고 |
|------|-----|------|
| 포텐시오미터 | AN0 | 10k, R33 브리지로 연결됨 |
| LED1 | P00.5 | Green, Low-active, 680 Ohm |

---

## 디버깅 팁

- winIDEA 디버거에서 `adcValue_AN0` 변수를 Watch 창에 추가하면 실시간 ADC 값 확인 가능
- ADC 값 범위: 0 ~ 4095 (12-bit)
- 포텐시오미터를 돌리면서 LED 점멸 속도 변화 확인

---

## 빌드 참고

- ADS IDE에서 프로젝트 새로고침(F5) 후 빌드
- VADC include 경로는 `.cproject`에 이미 설정됨
- `Drv_Adc.c`가 빌드에 자동 포함되지 않으면, 프로젝트 Properties → C/C++ Build에서 확인
